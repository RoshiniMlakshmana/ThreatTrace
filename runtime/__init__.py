"""ThreatTrace Tool Runtime Manager (Block 15L-16).

Deterministic dependency/runtime detection and bounded local demo
bootstrap. This package never executes an offensive scan itself, never
elevates privileges, never silently installs software, and never
bypasses analyst/admin approval — see `runtime.tool_runtime` and
`runtime.bootstrap` for the full boundary.
"""
